name: Fix Waveshare source attribution contract

on:
  push:
    branches:
      - 'work/waveshare/backend-parity'
    paths:
      - 'tools/fix_waveshare_source_attribution_contract.py'
      - '.github/workflows/fix-waveshare-source-attribution-contract.yml'

permissions:
  contents: write

jobs:
  update-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/waveshare/backend-parity
          fetch-depth: 0
      - name: Update source attribution contract for shared status ownership
        run: python3 tools/fix_waveshare_source_attribution_contract.py
      - name: Remove one-shot patch machinery
        run: |
          rm tools/fix_waveshare_source_attribution_contract.py
          rm .github/workflows/fix-waveshare-source-attribution-contract.yml
      - name: Commit contract update to lane branch
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'test: follow shared source-detection status ownership'
          git push origin HEAD:work/waveshare/backend-parity
